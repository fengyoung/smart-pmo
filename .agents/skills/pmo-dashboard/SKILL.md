---
name: pmo-dashboard
version: 1.1.0
description: "多项目概览仪表盘。展示所有关注项目的待办和里程碑状态汇总。从 Base 实时拉取数据。展示后自动检测告警项，提示是否下钻查看详情。"
metadata:
  requires:
    bins: []
  depends_on:
    - lark-base
---

# pmo-dashboard — 多项目概览

## 执行方式

```bash
claude pmo-dashboard
```

## 前置条件

至少有关注项目（已用 `pmo-pin` 关注）或 active 项目。

## 执行流程

1. 读取 `~/.smart-pmo/pinned` 获取关注项目列表
   - 如果 pinned 为空，使用所有 status=active 的项目
2. **并行**遍历每个项目，通过 `lark-base` 查询（每个项目一次查询，并发执行，不串行等待）：
   - 待处理数（状态=待处理）
   - 进行中数（状态=进行中）
   - 已过期数（截止日期 < currentDate 且 状态≠已完成/已取消）
   - 今日截止数（截止日期 = currentDate 且 状态≠已完成/已取消）
   - 里程碑进行中数
   - 里程碑即将到期数（7天内，截止日期 ≤ currentDate+7 且 状态≠已完成/已取消）
3. 汇总所有项目结果，展示概览：

```
📊 项目概览 · {today}
═══════════════════════════════
重点关注（{N} 个项目）：

◆ {项目名} ({代号})
  待办：待处理 {N} | 进行中 {N} | 今日截止 {N} | ⚠️ 过期 {N}
  里程碑：进行中 {N} | ⚠️ 即将到期 {N}

◆ {项目名} ({代号})
  待办：待处理 {N} | 进行中 {N} | 今日截止 {N} | ⚠️ 过期 {N}
  里程碑：进行中 {N} | ⚠️ 即将到期 {N}

使用 pmo-use <项目名> 查看详情
使用 pmo-pin <项目名>  关注更多项目
使用 pmo-search <关键词> --all  跨项目搜索

── 其他活跃项目（未关注）──
（仅当 pinned 不为空，且存在有告警的非关注 active 项目时展示）

  {项目名} ({代号})：过期 {N} | 今日截止 {N} | 里程碑到期 {N}
  {项目名} ({代号})：无告警 ✅

  使用 pmo-pin <项目名> 关注上述项目，纳入重点关注
```

4. **展示后自动检测告警项并提示下钻**：

收集满足以下任一条件的项目，按严重程度汇总告警：
- 有过期待办（过期数 > 0）
- 有今日截止待办（今日截止数 > 0）
- 有即将到期里程碑（即将到期数 > 0）

**若有告警项，展示完概览后立即输出下钻提示：**

```
─────────────────────────────────
⚠️ 发现 {N} 个项目需要关注：

  · {项目名}：{N} 个过期待办，{N} 个今日截止
  · {项目名}：{N} 个里程碑即将到期

是否查看详情？
  1. 查看所有告警项目的过期待办（pmo-todo-followup --all --overdue）
  2. 切换到 {最严重项目名} 查看待办详情
  3. 跳过
请输入选项编号，或直接回车跳过：
```

- 选择 `1`：执行 `pmo-todo-followup --all --overdue`
- 选择 `2`：执行 `pmo-use {项目名}` 后执行 `pmo-todo-followup --overdue`
- 选择 `3` 或直接回车：结束
- 若无告警项：静默结束，不显示下钻提示
