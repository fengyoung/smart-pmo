# Smart-PMO 项目需求清单

> 最后更新：2026-06-09

---

## 一、项目定位

Smart-PMO 是一套基于 **Claude Code Skill 系统 + 飞书 CLI + 飞书智能体 Bot** 的项目管理工具集，以半自动化的方式辅助单项目管理的日常运作。**所有飞书托管层的操作（定时推送、群聊交互）由 Bot 独立完成，无需自建服务。**通过一系列可复用的 Skill 覆盖会议纪要提取、待办追踪、群聊消息提取待办、文档归档、里程碑跟进等核心场景。

### 核心理念

- **多项目支持**：通过集中注册表管理多个项目，`pmo-use` 热切换上下文
- **配置驱动**：所有项目参数集中存储在 `~/.smart-pmo/registry/`，技能不硬编码
- **统一数据源**：多维表格 Base 是待办/里程碑/会议索引的唯一 truth，Bot 和 Claude Code 共享

---

## 二、整体架构

### 2.1 架构图

```
┌──────────────────────────────────────────────────────────────┐
│                    Claude Code (本地)                         │
│                                                              │
│  pmo-init            → 初始化项目+创建Base+知识库+注册         │
│  pmo-meeting-process → 读取妙记/文件 → 提取纪要→归档→写Base   │
│  pmo-todo-from-chat  → lark-im 读群消息 → 提取待办 → 写Base  │
│  pmo-todo-followup   → 查Base → 检查过期 → 推群卡片          │
│  pmo-archive         → 上传文件到知识库                       │
│  pmo-milestone       → 查Base里程碑 → 到期检查 → 推群        │
│  pmo-weekly-report   → 汇总Base → 生成周报 → 归档 → 推群     │
│                                                              │
│  项目上下文管理：                                              │
│  pmo-list / pmo-use / pmo-pin / pmo-dashboard                │
│                                                              │
│  依赖：lark-* 系列 skill + ~/.smart-pmo/registry/             │
└───────────────┬──────────────────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────────────────┐
│                    飞书 CLI 层                                │
│  lark-base / lark-doc / lark-wiki / lark-im / lark-minutes   │
└───────────────┬──────────────────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────────────────┐
│           飞书智能体 Bot (每项目独立) ║ 飞书平台托管           │
│                                                              │
│  定时推送：每日待办概览 / 里程碑到期提醒 / 检查                  │
│  指令交互：@Bot 自然语言查询 Base / 标记完成 / 新增待办         │
│  完全托管：无需自建服务器，纯飞书平台运行                       │
│  配置在 pmo-init 时录入 appId + appSecret                       │
└───────────────┬──────────────────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────────────────┐
│                     飞书数据层                                 │
│                                                              │
│  知识库(Wiki)        ← 会议纪要 + 周报 + 归档文档             │
│  多维表格(Base)      ← 待办 + 里程碑 + 会议索引（统一数据源）  │
│  群聊(IM)           ← 推送 + Bot指令交互 + 待办消息源         │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
项目群聊 → pmo-todo-from-chat → AI提取 → 用户确认 → 写Base → 推群反馈
飞书妙记 → pmo-meeting-process → AI提取 → 归档知识库 + 写Base → 推群反馈
外部转写文件 → pmo-meeting-process → AI提取 → 归档知识库 + 写Base → 推群反馈
@Bot /todo → Bot查Base → 直接回复群聊
定时任务 → Bot查Base → 推送概览卡片到群
```

---

## 三、配置与项目上下文管理

### 3.1 目录结构

```
~/.smart-pmo/                            ← 中央配置中心
├── registry/
│   ├── project-a.json                   ← project-a 的完整项目配置
│   ├── project-b.json                   ← project-b 的完整项目配置
│   └── ...                              ← 新项目由 pmo-init 自动注册
├── current                              ← 当前项目（文本文件，内容为项目名）
├── pinned                               ← 关注项目列表（每行一个项目名）
└── bot/
    └── config.json                      ← Bot 服务全局配置
```

### 3.2 项目配置 JSON (`~/.smart-pmo/registry/<project>.json`)

```json
{
  "project": {
    "name": "项目名称",
    "alias": "项目代号",
    "createdDate": "2026-06-09",
    "status": "active"
  },
  "team": {
    "pm": { "name": "项目经理", "openId": "ou_xxxx" },
    "members": [
      { "name": "成员A", "openId": "ou_xxxx", "role": "开发" },
      { "name": "成员B", "openId": "ou_xxxx", "role": "产品" }
    ]
  },
  "larkResources": {
    "wikiSpaceId": "ss_xxxxx",
    "baseAppToken": "bas_xxxxx",
    "baseTableIds": {
      "todos": "tbl_xxxxx",
      "milestones": "tbl_xxxxx",
      "meetingIndex": "tbl_xxxxx"
    },
    "chatIds": [
      "oc_xxxxxxxxxxxxxxxxxxxxxxxx"       // 项目群 ID（可多个）
    ],
    "bot": {
      "appId": "cli_xxxxx",               // 飞书智能体/自建应用 App ID
      "appSecret": "xxx",                 // App Secret
      "name": "project-a-pmo-bot",       // 智能体名称
      "chatId": "oc_xxxxxxxxxxxxxxxxxxxxxxxx"  // 绑定的群 ID
    }
  },
  "archive": {
    "directoryStructure": [
      "01-会议纪要",
      "02-周报",
      "03-需求文档",
      "04-设计文档",
      "05-项目资料",
      "99-归档"
    ]
  },
  "chat": {
    "lastReadMessageId": "om_xxxxx",      // 上次读取群消息的位置（排重用）
    "lastReadTime": "2026-06-08T18:00:00"
  }
}
```

### 3.3 当前项目确定规则

```
优先级：环境变量 > ~/.smart-pmo/current 文件
   1. 如果设了 `SMART_PMO_CURRENT=project-a` → 使用 project-a
   2. 否则读 `~/.smart-pmo/current` 文件内容
   
多终端互不干扰：终端1 export project-a, 终端2 export project-b
```

### 3.4 项目上下文管理 Skill

| Skill | 功能 |
|-------|------|
| `pmo-register` | 手动注册已有项目到注册表（通常由 pmo-init 自动完成，兜底用）|
| `pmo-use <项目名> [-g]` | 切换当前项目（-g 全局写入 current，默认只设环境变量）|
| `pmo-list` | 列出所有已注册项目及其状态概览 |
| `pmo-pin <项目名...>` | 关注 1-N 个项目，用于 dashboard 聚合展示 |
| `pmo-unpin <项目名>` | 取消关注项目 |
| `pmo-dashboard` | 一次性展示所有关注项目的待办/里程碑概览 |

---

## 四、数据存储方案

### 4.1 项目文档 → 飞书知识库（Wiki）

每个项目独立的知识空间，目录结构：

```
[项目名] 知识空间
├── 📁 01-会议纪要/         ← pmo-meeting-process 自动归档
│   ├── 2026-06-08-周例会.md
│   └── 2026-06-10-需求评审.md
├── 📁 02-周报/             ← pmo-weekly-report 生成
├── 📁 03-需求文档/
├── 📁 04-设计文档/
├── 📁 05-项目资料/
└── 📁 99-归档/
```

### 4.2 待办 + 里程碑 + 会议索引 → 飞书多维表格（Base）

每个项目一个 Base，包含 3 张数据表：

#### 📋 表1：待办事项（Todo Items）

| 字段名 | 字段类型 | 必填 | 说明 |
|--------|---------|------|------|
| `待办ID` | 自动编号 | — | 格式 TODO-001 |
| `待办内容` | 文本 | ✅ | 具体的待办事项描述 |
| `负责人` | 人员（单选） | ✅ | 唯一负责人 |
| `截止日期` | 日期 | ✅ | 格式 YYYY-MM-DD |
| `状态` | 单选 | ✅ | `待处理` / `进行中` / `已完成` / `已取消` |
| `优先级` | 单选 | ✅ | `P0-紧急` / `P1-重要` / `P2-一般` / `P3-低优` |
| `所属会议` | 关联记录 | | 关联到「会议记录索引」表 |
| `来源` | 文本 | | 标记来源：会议/群聊/手动 |
| `来源消息` | 文本 | | 群聊来源时记录消息 ID（去重用）|
| `完成日期` | 日期 | | 标记完成时自动填入 |
| `备注` | 文本 | | |
| `创建时间` | 创建时间（自动） | — | |
| `创建人` | 创建人（自动） | — | |

**可选视图：**
- 📌 我的待办（负责人=当前用户，状态≠已完成/已取消）
- ⚠️ 已过期（截止日期<今天，状态≠已完成/已取消）
- 📊 完成统计（按状态分组+计数）

#### 🏁 表2：里程碑（Milestones）

| 字段名 | 字段类型 | 必填 | 说明 |
|--------|---------|------|------|
| `里程碑ID` | 自动编号 | — | 格式 MILE-001 |
| `里程碑名称` | 文本 | ✅ | 如：需求评审完成、Beta 版发布 |
| `计划日期` | 日期 | ✅ | |
| `实际日期` | 日期 | | |
| `负责人` | 人员（单选） | ✅ | |
| `状态` | 单选 | ✅ | `未开始` / `进行中` / `已完成` / `已延期` / `已取消` |
| `进度` | 百分比 | | 0%~100% |
| `关联待办` | 关联记录 | | 关联到「待办事项」表 |
| `描述` | 文本 | | 详细说明/验收标准 |
| `备注` | 文本 | | |
| `创建时间` | 创建时间（自动） | — | |

**可选视图：**
- 📅 时间线（按计划日期排序，按状态分组）
- ⚠️ 即将到期（计划日期7天内，状态≠已完成）

#### 📝 表3：会议记录索引（Meeting Index）

| 字段名 | 字段类型 | 必填 | 说明 |
|--------|---------|------|------|
| `会议ID` | 自动编号 | — | 格式 MEET-001 |
| `会议日期` | 日期 | ✅ | |
| `会议主题` | 文本 | ✅ | |
| `开始时间` | 日期时间 | | |
| `结束时间` | 日期时间 | | |
| `参会人` | 人员（多选） | | |
| `记录方式` | 单选 | ✅ | `飞书妙记` / `外部转写` / `手动记录` |
| `来源文件` | 文本 | | 妙记链接或本地转写文件路径 |
| `纪要文档链接` | 链接 | ✅ | 归档后的文档链接 |
| `产出待办` | 关联记录 | | 关联到「待办事项」表，1 → N |
| `讨论要点摘要` | 文本 | | AI 自动提取 |
| `关键决策` | 文本 | | AI 自动提取 |
| `备注` | 文本 | | |
| `创建时间` | 创建时间（自动） | — | |

**可选视图：**
- 📂 全部记录（按日期降序）

#### 表间关联

```
会议记录索引 ──(产出待办)──→ 待办事项
     │
     │ (关联待办)
     └───────────────→ 里程碑
```

---

## 五、功能模块详情

### M1 — `pmo-init` 项目初始化（P0）

交互式创建新项目，自动完成全部基础设施搭建。

| # | 需求 |
|---|------|
| 1 | 交互式收集：项目名称、项目经理、核心成员、项目群 |
| 2 | 自动创建**飞书知识空间**（独立），按模板建立目录结构（6 个目录） |
| 3 | 自动创建**多维表格 Base**（3 张表：待办/里程碑/会议索引），按 4.2 定义字段 |
| 4 | 生成配置写入 `~/.smart-pmo/registry/<项目名>.json` |
| 5 | 设定 `~/.smart-pmo/current` 指向新项目 |
| 6 | 初始化完成后将配置摘要推送到项目群 |

### M2 — `pmo-meeting-process` 会议处理（P0）

| # | 需求 |
|---|------|
| 1 | **输入源A（飞书妙记）**：通过妙记链接或 session ID 自动获取转写内容（调 `lark-minutes`） |
| 2 | **输入源B（外部转写文件）**：传入本地转写文件路径（.txt / .docx / .md），Claude 解析 |
| 3 | 自动识别并提取：会议主题、日期时间、参会人 |
| 4 | 自动提取：讨论要点、关键决策、争议点 |
| 5 | 自动提取：待办事项（内容、负责人、截止时间） |
| 6 | 生成结构化会议纪要文档，归档到知识库 `01-会议纪要/` |
| 7 | 将提取的待办写入 Base「待办事项」表，关联对应的会议记录 |
| 8 | 将会议记录写入 Base「会议记录索引」表 |
| 9 | 将纪要和待办摘要推送到项目群（P1 推送卡片）|
| 10 | 支持追加补充纪要内容 |

### M3 — `pmo-todo-from-chat` 群消息提取待办（P0）

| # | 需求 |
|---|------|
| 1 | 通过 `lark-im` 从项目群读取未处理的历史消息（从 `lastReadMessageId` 开始）|
| 2 | 最多读取 100 条消息，过滤系统消息和 Bot 交互消息 |
| 3 | AI 分析消息内容，识别潜在待办（基于语义模式：提及责任人、任务表述等）|
| 4 | 提取待办要素：内容、负责人、隐式截止时间 |
| 5 | 与 Base 已有待办排重（基于内容相似度 >80% 标记重复）|
| 6 | 展示给用户确认后写入 Base「待办事项」表 |
| 7 | 更新 `lastReadMessageId` 和 `lastReadTime`，避免重复处理 |
| 8 | 写入完成后推送群消息反馈 |

**当前阶段仅支持手动触发**：`pmo-todo-from-chat`

### M4 — `pmo-todo-followup` 待办跟进（P0）

| # | 需求 |
|---|------|
| 1 | 从 Base 读取当前有效待办（状态为"待处理"/"进行中"），支持按负责人或截止日期分组 |
| 2 | 检查已过期待办并生成提醒（P0 红色告警卡片）|
| 3 | 标记待办为"已完成"，自动填入完成日期 |
| 4 | 修改待办负责人或截止日期 |
| 5 | 推送待办状态摘要到项目群 |

### M5 — `pmo-archive` 文档归档（P0）

| # | 需求 |
|---|------|
| 1 | 上传本地文件到知识库指定目录（Word/PDF/图片/代码文档等）|
| 2 | 归档时自动添加元数据：归档日期、类型 |
| 3 | 归档记录写入 Base「会议记录索引」表（可选）|

### M6 — `pmo-milestone` 里程碑管理（P1）

| # | 需求 |
|---|------|
| 1 | 从 Base 读取所有里程碑及当前状态 |
| 2 | 新增/修改里程碑（写入 Base）|
| 3 | 检查即将到期（7天内）和已过期的里程碑，推送提醒（P1 告警卡片）|
| 4 | 标记里程碑完成，记录实际完成日期 |

### M7 — `pmo-weekly-report` 周报生成（P1）

| # | 需求 |
|---|------|
| 1 | 统计本周会议次数和主要议题 |
| 2 | 统计待办完成率（本周新增 / 本周完成 / 逾期未完成）|
| 3 | 统计里程碑状态 |
| 4 | 生成格式化周报文档，归档到知识库 `02-周报/` |
| 5 | 推送周报摘要到项目群 |

---

## 六、群聊设计（完整）

### 6.1 数据流

```
                   项目群聊
          ┌──────────────────────────┐
          │  讨论、决策、@Bot交互...   │
          └──┬───────────────┬───────┘
             │               │
             │ lark-im 读取   │ @Bot 指令
             ▼               ▼
     ┌──────────────┐  ┌─────────────┐
     │ Claude Code  │  │ 飞书 Bot    │
     │ (AI提取待办) │  │ (直接操作)   │
     └──────┬───────┘  │ 查/写Base    │
            │          │ 回复群卡片   │
            │ 写Base   └──────┬──────┘
            ▼                 │
     ┌────────────────────────┘
     ▼
  多维表格 Base（统一数据源）
```

### 6.2 A — 推送内容规范

#### 推送优先级分级

| 优先级 | 推送事件 | 卡片类型 | 推送时机 |
|--------|---------|---------|---------|
| **P0 紧急** | 待办已过期、里程碑已过期 | ⚠️ 红色告警卡片 | 发现即推送 |
| **P1 重要** | 会议纪要发布、待办即将到期（24h内）、里程碑即将到期（7天内）| 📋 信息摘要卡片 | 事件发生后推送 |
| **P2 例行** | 周报、每日概览、里程碑完成确认 | 📊 汇总卡片 | 定时按计划推送 |

#### 卡片消息模板

**⚠️ P0 过期待办提醒：**
```
┌─────────────────────────────────────────┐
│ ⚠️ 过期待办提醒                          │
│                                         │
│ project-a 有以下待办已逾期：              │
│                                         │
│ □ TODO-003 | 确定 UI 方案 → 小王 逾期3天 │
│ □ TODO-005 | 后端联调    → 小李 逾期1天 │
│                                         │
│ [查看全部待办]                           │
│ ———————————————————————————              │
│ Smart-PMO · 2026-06-09 10:00            │
└─────────────────────────────────────────┘
```

**📋 P1 会议纪要发布：**
```
┌─────────────────────────────────────────┐
│ 📋 会议纪要已发布                        │
│                                         │
│ 会议：2026-06-09 周例会                  │
│ 产出待办：3 项                           │
│                                         │
│ 关键决策：                               │
│ • 确定采用方案B                          │
│ • v2.0 延期至 7 月                       │
│                                         │
│ [查看完整纪要]                           │
│ ———————————————————————————              │
│ Smart-PMO · 2 分钟前                     │
└─────────────────────────────────────────┘
```

**📊 P2 每日概览：**
```
┌─────────────────────────────────────────┐
│ 📊 project-a 项目概览 · 2026-06-09      │
│                                         │
│ 待办：5 待处理 | 3 进行中 | 2 已过期     │
│ 里程碑：1 即将到期（3天）                │
│                                         │
│ [查看详情] [切换项目]                    │
│ ———————————————————————————              │
│ Smart-PMO · 每日 10:00 自动推送          │
└─────────────────────────────────────────┘
```

#### 推送时机与频率

| 推送方 | 触发条件 | 频率限制 |
|--------|---------|---------|
| Claude Code | Skill 执行完毕后自动推送 | 按需 |
| 智能体 Bot 定时 | 每日 10:00 待办概览 | 每项目每天最多 1 次 |
| 智能体 Bot 定时 | 每日 09:30 里程碑检查 | 每项目每天最多 1 次 |

### 6.3 B — 群聊消息自动提取待办

#### `pmo-todo-from-chat` 执行流程

```
用户执行 `pmo-todo-from-chat`
         │
         ▼
① 从 `pmo-config.json` 读 lastReadMessageId
   └→ 调 lark-im list_message 获取后续消息（上限 100 条）
   └→ 过滤：排除系统消息、Bot 交互消息（@Bot 的指令和回复）
         │
         ▼
② AI 分析消息内容，提取潜在待办
   └→ 识别模式："我来负责" "x总处理一下" "需要做" "明天前要完成" 等
   └→ 提取：待办内容、负责人（@提及/姓名）、隐式截止时间（"这周五"）
         │
         ▼
③ 与 Base 已有待办排重
   └→ 来源消息 ID 去重：同一消息不重复提取
   └→ 内容去重：语义相似度 >80% 跳过
         │
         ▼
④ 展示给用户确认
   └→ "从群消息中发现以下 3 条潜在待办，是否写入 Base？"
   └→ [全部写入] [选择写入] [忽略全部]
         │
         ▼
⑤ 确认后 → 通过 lark-base 写入「待办事项」表
   更新 ~/.smart-pmo/config 中的 lastReadMessageId
   推群反馈：[已从群消息提取 3 条待办]
```

#### 排重机制

| 场景 | 排重策略 |
|------|---------|
| 同一消息多次读取 | 通过 `lastReadMessageId` 增量读取，不重复 |
| 同一待办在群聊中反复讨论 | 基于待办内容的语义相似度去重 |
| 已录入 Base 的待办又被识别 | 比对 Base 已有记录标题 -> 跳过 |
| 误提取（非待办的日常对话）| 用户确认环节兜底，不强制写入 |

### 6.4 C — 飞书智能体 Bot 操作 Base

#### Bot 技术方案

| 项目 | 选型 |
|------|------|
| 运行方式 | **飞书智能体平台**托管，无需自建服务 |
| 创建方式 | 用户在飞书开放平台手动创建自建应用，pmo-init 录入 appId/appSecret |
| 数据源 | 绑定到项目对应的多维表格 Base |
| 交互方式 | 自然语言对话（@Bot 提问），平台自带 LLM 理解能力 |

#### Bot 能力清单

用户在飞书开放平台创建自建应用后，在智能体配置中绑定 Base 并配置以下能力：

**① 群聊交互能力（自然语言对话）**

| 能力 | 交互示例（自然语言）| 绑定数据源 |
|------|-------------------|-----------|
| 查询我的待办 | `@SmartPMO 我的待办有哪些` | Base「待办事项」表 |
| 查询过期待办 | `@SmartPMO 有哪些已过期的待办` | Base「待办事项」表 |
| 查询里程碑 | `@SmartPMO 里程碑状态怎么样了` | Base「里程碑」表 |
| 标记待办完成 | `@SmartPMO 把 TODO-003 标记为完成` | Base「待办事项」表（写）|
| 新增待办 | `@SmartPMO 帮我加一个待办：完成方案评审，负责人小王，截止6月15日` | Base「待办事项」表（写）|
| 转交待办 | `@SmartPMO 把 TODO-003 转给小王` | Base「待办事项」表（写）|
| 项目概览 | `@SmartPMO 项目现在整体情况怎么样` | Base 全表查询 |

> 智能体使用自然语言交互，非结构化指令。上表为预期能力，实际可用程度取决于飞书智能体平台对 Base 的操作能力，设计阶段需验证。

**② 定时推送能力（智能体后台配置）**

| 定时任务 | 触发时间 | 动作 | 说明 |
|---------|---------|------|------|
| 每日待办概览 | 工作日 10:00 | 查待办表 → 推送待办概览卡片到群 | 含待处理数、进行中数、过期数 |
| 里程碑检查 | 工作日 09:30 | 查里程碑表 → 推送里程碑状态到群 | 含即将到期（7天内）和已过期的里程碑 |
| 过期待办提醒 | 工作日 10:00 | 与待办概览合并 | 在概览卡片中突出显示过期项 |

#### 智能体配置步骤（用户手动完成）

```
① 飞书开放平台 → 创建自建应用 → 获取 appId + appSecret
     ↓
② 开通权限：bitable:record:read, bitable:record:write, im:message
     ↓
③ 发布应用 → 添加飞书智能体能力
     ↓
④ 在智能体配置中添加 Base 数据源（指向项目 Base）
     ↓
⑤ 在智能体后台配置定时任务：
   - 每日 10:00 → 查待办表 → 推概览卡片到群
   - 每日 09:30 → 查里程碑表 → 推检查卡片到群
     ↓
⑥ 将 Bot 拉入项目群
     ↓
⑦ 在 pmo-init 中录入 appId + appSecret + 绑定的群 ID
```

#### 权限声明

自建应用在飞书开放平台需申请权限：
- `bitable:record:read` — 读 Base 数据
- `bitable:record:write` — 写 Base 数据
- `im:message` — 收/发群消息

#### 多项目支持

每个项目使用**独立的**飞书智能体 Bot，天然上下文隔离：
- 项目 A → Bot A → 绑定项目 A 的 Base
- 项目 B → Bot B → 绑定项目 B 的 Base
- 各 Bot 互不干扰，不需要 chat_id 映射逻辑

在 `~/.smart-pmo/registry/<project>.json` 中记录对应 Bot 的 appId 和 appSecret。

---

## 七、非功能需求

| # | 需求 | 说明 |
|---|------|------|
| NF1 | **多项目支持** | 集中注册表管理，`pmo-use` 热切换，互不干扰 |
| NF2 | **配置驱动** | 所有项目参数集中在 `~/.smart-pmo/registry/` |
| NF3 | **交互友好** | 关键操作前确认；输出格式化表格/卡片 |
| NF4 | **可扩展** | 新增 Skill 统一命名和接口风格 |
| NF5 | **飞书 CLI 依赖** | 飞书操作通过 `lark-*` skill 完成 |
| NF6 | **幂等安全** | 重复执行同一次会议/消息处理不产生重复数据 |

---

## 八、Skill 命名总览

| Skill | 功能 | 优先级 | 指令示例 |
|-------|------|--------|---------|
| `pmo-init` | 项目初始化 | P0 | `claude pmo-init` |
| `pmo-use` | 切换当前项目 | P0 | `claude pmo-use project-a` |
| `pme-list` | 列出所有项目 | P0 | `claude pmo-list` |
| `pmo-pin` / `pmo-unpin` | 关注项目管理 | P1 | `claude pmo-pin project-a project-b` |
| `pmo-dashboard` | 多项目概览 | P1 | `claude pmo-dashboard` |
| `pmo-meeting-process` | 会议处理 | P0 | `claude pmo-meeting-process --minutes <url>` |
| `pmo-todo-from-chat` | 群消息提取待办 | P0 | `claude pmo-todo-from-chat` |
| `pmo-todo-followup` | 待办跟进 | P0 | `claude pmo-todo-followup` |
| `pmo-archive` | 文档归档 | P0 | `claude pmo-archive --file <path> --dir <目录>` |
| `pmo-milestone` | 里程碑管理 | P1 | `claude pmo-milestone --check` |
| `pmo-weekly-report` | 周报生成 | P1 | `claude pmo-weekly-report` |

## 九、实施路线

| 阶段 | 内容 | 目标 |
|------|------|------|
| **阶段一（P0）** | pmo-init + pmo-meeting-process + pmo-todo-from-chat + pmo-todo-followup + pmo-archive + pmo-use/pmo-list + 智能体 Bot 配置指引文档 | 核心闭环可用 + 定时推送配置到位 |
| **阶段二（P1）** | pmo-milestone + pmo-weekly-report + pmo-dashboard/pmo-pin | 管理维度完整 |
| **阶段三（P2）** | 飞书智能体 Bot 深度增强（更多 Base 操作能力验证与调优）| 交互体验提升 |

---

## 十、待设计阶段讨论（已确认）

以下问题在设计阶段进一步细化：

1. ~~Bot 多项目方案~~ → **已确认：每个项目独立的飞书智能体 Bot** ✅
2. ~~Bot 部署方案~~ → **已确认：飞书平台托管，不自建服务** ✅
3. ~~定时推送~~ → **已确认：飞书智能体 Bot 独立完成定时推送** ✅
4. **待设计阶段细化：**
   - `larkResources.chatIds` 是否支持多个群（一个项目多个群）
   - 外部转写文件支持的格式范围（txt / docx / md / srt / 其他）
   - 卡片消息构建方式：通过 lark-im skill 直接推送
